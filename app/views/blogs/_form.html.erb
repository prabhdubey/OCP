
<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title,  :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.cktext_area :body , :class => 'ckeditor' %><br>
  </div>
   <div class="field">
   <%= f.file_field :image %>
  </div>

 <div class="field">
     <%= f.check_box :flag %> &nbsp;&nbsp;&nbsp;<%= f.label "Publish" %> 
</div>
  <div class="actions">
    <%= f.submit 'Create' ,:class => 'btn btn-info'%>&nbsp;&nbsp;&nbsp;
    <%= link_to 'Back', blogs_path , :class => 'btn btn-warning'%>
  </div>
<% end %>

